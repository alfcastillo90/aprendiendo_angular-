import { PruebasDirective } from './pruebas.directive';

describe('PruebasDirective', () => {
  it('should create an instance', () => {
    const directive = new PruebasDirective();
    expect(directive).toBeTruthy();
  });
});
